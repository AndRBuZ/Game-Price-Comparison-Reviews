.grid.gap-4.sm:grid-cols-1.md:grid-cols-1.lg:grid-cols-1
  - games.each do |game|
    .game-item id="game-#{game.id}"
      = link_to game.name, game_path(game), class: "block p-4 bg-white rounded-lg shadow-md hover:shadow-lg transition duration-300 ease-in-out text-center"
